{
  "name": "signalk-bridge",
  "version": "0.1.0",
  "description": "Node.js bridge implementing the SignalK plugin app API surface for signalk-rs",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/"
  },
  "keywords": ["signalk", "signalk-rs", "bridge"],
  "license": "Apache-2.0",
  "dependencies": {
    "express": "^4.18.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
